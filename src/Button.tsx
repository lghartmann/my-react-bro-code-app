function Button() {
  const styles = {
    backgroundColor: "lightcoral",
    color: "white",
    borderRadius: "6px",
    border: "none",
    padding: "12px 16px",
    cursor: "pointer",
  };
  return <button style={styles}>Click me</button>;
}

export default Button;
